# 实验报告：RAG 检索组件的对照评估

> 日期：2026-08-17 · 环境：RTX 4060 Laptop (8GB) / PyTorch 2.5.1+cu121
> 原始数据：`outputs/results.csv` · 图表：`outputs/fig*.png`

## 1. 实验设置

### 1.1 语料与评测集

- 语料：12 篇高校制度文档（培养方案 / 课程大纲 / 保研 / 奖学金 / 转专业 / 辅修 / 国际交流 / 选课规定），主题相近、条件数字交叉（如绩点 2.5 / 3.0 / 3.3 分属不同制度），构成天然干扰。
- 评测集：**38 题**人工标注（18 基础题 + 20 干扰题），每题含问题、相关来源、参考答案。问题风格刻意混合：直问术语型（BM25 友好）与口语改写型（向量检索友好）。

### 1.2 指标口径

相关性判在**来源文档级**（跨分块策略可比的前提，详见架构设计 §2.4）。

| 指标 | 定义 | 关注点 |
| --- | --- | --- |
| hit@1 / recall@1 | 第一名是否命中相关来源 | 严格档，区分度主指标 |
| recall@5 | top-5 覆盖的相关来源比例 | 召回完整性 |
| mrr@5 | 首个相关结果排名倒数均值 | 排序质量 |
| latency_ms | 检索+重排耗时（不含生成） | 体验指标 |

### 1.3 实验矩阵（控制变量法，四组 12 配置）

- A 分块策略：fixed / recursive / sentence（size=400 对齐）
- B 块大小：200 / 400 / 600（recursive）
- C 检索消融：混合检索 × 重排（2×2）
- D 向量模型：bge-small-zh-v1.5 (512d) vs bge-base-zh-v1.5 (768d)

## 2. 结果总表

| 配置 | hit@1 | recall@5 | mrr@5 | 延迟(ms) |
| --- | --- | --- | --- | --- |
| strategy=fixed | 0.842 | **1.000** | 0.917 | 46.9 |
| strategy=recursive | 0.842 | **1.000** | 0.917 | 42.5 |
| strategy=sentence | 0.842 | **1.000** | 0.917 | 41.8 |
| size=200 / 400 / 600 | 0.842 | **1.000** | 0.917 | 39-42 |
| hybrid=on, rerank=on | 0.842 | **1.000** | 0.917 | 41.6 |
| **hybrid=on, rerank=off** | **0.895** | 0.974 | **0.928** | **7.8** |
| hybrid=off, rerank=on | 0.842 | **1.000** | 0.917 | 43.7 |
| hybrid=off, rerank=off | 0.868 | 0.974 | 0.921 | **7.3** |
| bge-small / bge-base | 0.842 | **1.000** | 0.917 | 40-42 |

## 3. 发现与分析

### 发现 1：来源级 recall@5 全线饱和

除关闭重排的两格（0.974）外全部 1.000。**这不是"系统完美"，而是"评测太容易"**：12 个来源、top-5 覆盖率 42%，主题清晰的语料上粗排就能进前五。诚实的结论是——该口径下 recall@5 失去区分度，有效信息集中在 hit@1 与 MRR（这也是本报告同时报告 @1 档的原因）。后续工作应引入块级判定与更大规模干扰语料。

### 发现 2：重排的两面性（本报告最有价值的发现）

| 对比 | hit@1 | recall@5 | mrr@5 | 延迟 |
| --- | --- | --- | --- | --- |
| RRF 直出（hybrid=on） | **0.895** | 0.974 | **0.928** | 7.8ms |
| + bge-reranker 精排 | 0.842 | **1.000** | 0.917 | 41.6ms |

重排把 top-5 召回补满（+2.6pp）——交叉编码器确实能把深埋的相关块捞进候选；**但 top-1 精度反而下降 5.3pp，且延迟 ×5.3**。

坏例分析（抽查 confusable 题）：交叉编码器对"数字+同词"型干扰抵抗力弱——问"辅修绩点要求"时，含"绩点不低于 3.0"的国际交流块与含"绩点不低于 2.5"的辅修块词面高度重叠，reranker 给前者打出更高分。而 RRF 融合的两路排名里，正确来源因语义信号更集中而居前。

**工程结论**：重排不是无脑开关。追求"前五必有"（如引用溯源场景）开重排；追求"第一条就对"且对延迟敏感（如快速问答）可只用混合检索。多路精排（rerank 分数与 RRF 分数加权融合）是直接的改进方向。

### 发现 3：延迟结构——重排是本地管线的延迟主体

纯检索（BM25+向量+RRF）约 7-8ms，加重排后 40ms+，其中约 34ms 花在 20 对交叉编码上。生成端（API）另计 1-3s。若要优化端到端延迟，优先级：候选数 20→10（重排耗时近似线性）> 换更小重排模型 > 精简 embedding。

### 发现 4：bge-small 与 bge-base 指标打平

38 题上两模型 hit@1 / MRR 完全一致。该语料规模下向量模型不是瓶颈——**资源受限部署（纯 CPU / 树莓派级）选 small（参数量 1/3、维度 512）零损失**。指标打平的另一面是评测区分度不足（同发现 1），不构成"两模型等价"的一般结论。

### 发现 5：分块策略与块大小在来源级不敏感

本语料段落天然较短（多数 <400 字），三种策略产出的块集合高度相似（recursive 与 fixed 同为 57 块；size=200 为 62 块），来源级指标无差异。**递归分块的价值在块语义完整性**（不拦腰截断句子），这影响下游生成质量而非来源级检索指标——验证它需要块级评估或端到端答案质量评估，列为后续工作。

## 4. 结论

1. 当前语料+口径下的**推荐配置**：recursive/400 + hybrid=on + rerank=on + bge-small（重排开关按场景）。
2. 系统性收获：**评测口径决定结论**——来源级 recall@5 在小语料上会掩盖组件差异，hit@1/MRR + 消融才能暴露重排的真实代价。
3. 后续工作：块级相关性评估；rerank 分数与 RRF 的加权融合；端到端答案质量评估（faithfulness/幻觉率，需 LLM-as-judge）；更大规模干扰语料。
